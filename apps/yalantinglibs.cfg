[project_info]
APP_NAME=yalantinglibs
APP_VERSION=0.5.5
REPO_URL=https://github.com/alibaba/yalantinglibs.git

PROP_DISABLE_WINDOWS=YES

CMD_EXEC_DIR=${RCB_APP_BUILD_DIR}
CMD_CLEAN = cmake --build --target --clean
CMD_CMAKE_CONFIG=-DCMAKE_INSTALL_PREFIX=${ROCM_HOME} ${RCB_APP_SRC_DIR}
